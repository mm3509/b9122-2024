#include <stdio.h>

int count_steps(int n) {
  // TODO: complete this function. Autograder will only use this function.
  return 0;
}

int main() {
  // Note: Autograder will ignore this function.
  for (int i = 1; i <= 8; i++) {
    printf("Number of ways to go up %d steps: %d\n", i, count_steps(i));
  }
  return 0;
}
